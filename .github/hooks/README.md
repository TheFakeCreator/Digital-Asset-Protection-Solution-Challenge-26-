# Copilot Hooks

This folder contains Copilot hook configuration files (`*.json`).

## Active Hook Config

- `safety-and-context.json`

The configuration runs scripts from `scripts/hooks/` to:
- inject a short session-start reminder
- block potentially destructive tool commands before execution
